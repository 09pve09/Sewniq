console.log('ROUTES RE HERE');
var users = require('../controllers/users.js');
module.exports = function(app){
  // app.get('/questions', qa.questions);
  // app.post('/users', users.create);
  // app.post('/questions', qa.createq);
  // app.get('/questions/:id', qa.showone);
  // app.post('/answers/:id', qa.createa);
  // app.post('/likes/:id', qa.update);
  // app.post('/friends', friends.create);
  // app.put('/friends/:id', friends.update);
  // app.delete('/friends/:id', friends.delete);
}
// this adds route listeners to friends for 5 of the 7 RESTful routes, excluding new and edit.
